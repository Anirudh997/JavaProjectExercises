/*public class ComparableExercise implements Comparable<Employee>{


    String empName;
    Integer empId;

    // getters and setters

    public int compareTo(Employee emp){
        // write the logic or you can use your Custom Comparator with Lambda expression
        if(this.empId > emp.empId){
            return 1;
        }else if(this.empId < emp.empId){
            return -1;
        }
        return 0;
    }
}
*/




